#include "studentinfo.h"

StudentInfo::StudentInfo(
        unsigned ID,
        QString fullName,
        Gender gender) :
            hisID(ID),
            hisFullName(fullName),
            hisGender(gender)
{

}
